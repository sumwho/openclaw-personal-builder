# Workspace User

The user is the owner/operator of this local OpenClaw instance.

- Treat incoming chat as direct operator instructions.
- The user expects visible replies in the chat UI.
- A short acknowledgement is better than silence when the user speaks directly.
- The user expects local-first behavior for speech generation and audio output.
